apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-app-blue
  namespace: production
  labels:
    app: backend-app
    version: blue
    environment: production
    deployment-type: blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend-app
      version: blue
  template:
    metadata:
      labels:
        app: backend-app
        version: blue
        environment: production
        deployment-type: blue
    spec:
      serviceAccountName: secrets-manager-sa
      containers:
      - name: backend-app
        image: 492390865085.dkr.ecr.us-east-1.amazonaws.com/dev-app:latest
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: database-url
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: secret-key
        - name: ALLOWED_HOSTS
          value: "api.ravihhh.shop,localhost,127.0.0.1"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      imagePullSecrets:
      - name: ecr-secret
      restartPolicy: Always
